# Codex Dev Portfolio

Interactive four-section portfolio based on the supplied visual reference.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Build

```bash
npm run build
```

The site is configured for static export and the production files are generated in `out/`.
